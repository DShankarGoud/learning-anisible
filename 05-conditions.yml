- name: Condition
  hosts: localhost
  tasks:
    # Number Comparison
    - name: Print if a == 10
      debug:
        msg: Yes  a is 10
      when: a == "10"

    - name: Print if a > 10
      debug:
        msg: Yes  a is greater than 10
      when: a > "10"
